---
'pinnace': patch
---

Bump the emitted cloud-init's default on-box pinnace version (`DEFAULT_PINNACE_VERSION`) to `0.6.0`, the release that adds `pinnace pin --from-ipns <name>` (migrate an existing IPNS site onto your own nodes + your own IPNS name in one command).
