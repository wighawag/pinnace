---
'pinnace': patch
---

Bump the emitted cloud-init's default on-box pinnace version (`DEFAULT_PINNACE_VERSION`) to `0.3.5`, the release that resolves the on-box `pinnace` bin via PATH so the node timers do not 203/EXEC on a nodesource npm prefix.
