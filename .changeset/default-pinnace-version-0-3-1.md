---
'pinnace': patch
---

Bump the emitted cloud-init's default on-box pinnace version (`DEFAULT_PINNACE_VERSION`) to `0.3.1`, the release that fixes the `Provide.Interval --json` boot-abort and guarantees the Kubo datadir before the sandboxed daemon starts.
