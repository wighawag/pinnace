---
'pinnace': minor
---

Scaffold the `pinnace` package: a pnpm-workspace member (ESM, NodeNext, strict TS, tsc→dist, vitest, changesets) exposing BOTH a library API via `exports["."]` and a thin `pinnace` bin. Establishes the core-vs-cli seam every subsequent tracer-bullet task builds on. Licensed AGPL-3.0-only.
