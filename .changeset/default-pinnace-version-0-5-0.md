---
'pinnace': patch
---

Bump the emitted cloud-init's default on-box pinnace version (`DEFAULT_PINNACE_VERSION`) to `0.5.0`, the release that adds `pinnace pin <cid> --as <name>` (pin an arbitrary external network CID redundantly across nodes, tracked in MFS).
